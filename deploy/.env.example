# =====================================================
# CrossBot 本番環境 環境変数テンプレート
# =====================================================
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env

# -----------------------------------------------------
# データベース設定
# -----------------------------------------------------
DB_USER=crossbot
DB_PASSWORD=your_secure_password_here
DB_NAME=crossbot

# Prisma用（Docker Compose内では自動設定）
# DATABASE_URL=postgresql://crossbot:password@postgres:5432/crossbot

# -----------------------------------------------------
# 認証設定
# -----------------------------------------------------
# JWT署名用シークレット（32文字以上のランダム文字列）
# 生成コマンド: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_minimum_32_characters

# -----------------------------------------------------
# OpenAI API設定
# -----------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------
# CORS設定
# -----------------------------------------------------
# 許可するオリジン（カンマ区切り）
CORS_ORIGINS=https://chat.example.com,https://admin.example.com,https://your-lms-site.com

# -----------------------------------------------------
# 通知設定（オプション）
# -----------------------------------------------------
# Slack Webhook URL（新規問い合わせ通知用）
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# -----------------------------------------------------
# その他
# -----------------------------------------------------
NODE_ENV=production
